---
#
# Documentation:
# https://docs.github.com/en/actions/writing-workflows/workflow-syntax-for-github-actions
#

name: Build & Deploy

on:
  push:
    branches:
      - alpha
      - dev
      - main
  release:
    types: ['created']

permissions:
  contents: read

jobs:
  deploy_alpha:
    name: Build & Deploy alpha
    runs-on: ubuntu-latest
    environment: release
    if: github.event_name == 'push' && (github.ref == 'refs/heads/alpha' || github.ref == 'refs/heads/dev')
    permissions:
      contents: read
      id-token: write # Required for OIDC (npm Trusted Publishers)
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # Setup .npmrc file to publish to npm
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24.x'
          registry-url: 'https://registry.npmjs.org'
          # zizmor: ignore[use-trusted-publishing] already using OIDC (id-token: write) with plain `npm publish`; always-auth/registry-url still configure the registry endpoint, no static token is used
          always-auth: true
          package-manager-cache: false
          # Defaults to the user or organization that owns the workflow file
          scope: 'nvuillam'

      - run: yarn install --frozen-lockfile
      - run: yarn config set version-git-tag false
      - run: ALPHAID=$(date '+%Y%m%d%H%M') && yarn version --prepatch --preid="alpha$ALPHAID"
      # Use npm publish for OIDC / npm Trusted Publishers
      - run: npm publish --tag alpha

  deploy_beta:
    name: Deploy beta
    runs-on: ubuntu-latest
    environment: release
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      id-token: write # Required for OIDC (npm Trusted Publishers)
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # Setup .npmrc file to publish to npm
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24.x'
          registry-url: 'https://registry.npmjs.org'
          # zizmor: ignore[use-trusted-publishing] already using OIDC (id-token: write) with plain `npm publish`; always-auth/registry-url still configure the registry endpoint, no static token is used
          always-auth: true
          package-manager-cache: false
          # Defaults to the user or organization that owns the workflow file
          scope: 'nvuillam'

      - run: yarn install --frozen-lockfile
      - run: yarn config set version-git-tag false
      - run: BETAID=$(date '+%Y%m%d%H%M') && yarn version --prepatch --preid="beta$BETAID"
      # Use npm publish for OIDC / npm Trusted Publishers
      - run: npm publish --tag beta

  deploy_release:
    name: Deploy release
    runs-on: ubuntu-latest
    environment: release
    if: github.event_name == 'release'
    permissions:
      contents: read
      id-token: write # Required for OIDC (npm Trusted Publishers)
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # Setup .npmrc file to publish to npm
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24.x'
          registry-url: 'https://registry.npmjs.org'
          package-manager-cache: false
          # Defaults to the user or organization that owns the workflow file
          scope: 'nvuillam'

      - run: yarn install --frozen-lockfile
      # Use npm publish for OIDC / npm Trusted Publishers
      - run: npm publish
